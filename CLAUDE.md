# CLAUDE.md

This is the root of the "Aina-bakery" repo. The actual game project ("Engine Noname") lives in `New folder (2)/`.

**At the start of every new session/chat room, before anything else:**

1. Read `New folder (2)/ตัวตนไอ.txt` (ไอ's persona/tone).
2. Read `New folder (2)/Workflow Rules.txt` and `New folder (2)/Report Format.txt` (working rules + report format).
3. Read `New folder (2)/sim_state.json` and `New folder (2)/sim_report.json` (current game state / most recent day's result).
4. Produce a status report for the current day using the exact structure in `Report Format.txt`, in the tone defined by `ตัวตนไอ.txt`, following the rules in `Workflow Rules.txt`.

See `New folder (2)/CLAUDE.md` for full engine/architecture documentation.
